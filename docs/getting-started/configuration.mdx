---
title: Configuration
description: Environment variables and configuration options for Briefly CRM.
---

# Configuration

## Environment Variables

Create a `.env` file based on `.env.example`:

### Database

| Variable       | Description                  | Required |
| -------------- | ---------------------------- | -------- |
| `DATABASE_URL` | PostgreSQL connection string | Yes      |

### Authentication

| Variable             | Description                                 | Required |
| -------------------- | ------------------------------------------- | -------- |
| `BETTER_AUTH_SECRET` | Secret key for Better Auth (32+ characters) | Yes      |
| `BETTER_AUTH_URL`    | Base URL for the application                | Yes      |

### OAuth (Optional)

| Variable               | Description                | Required |
| ---------------------- | -------------------------- | -------- |
| `GOOGLE_CLIENT_ID`     | Google OAuth client ID     | No       |
| `GOOGLE_CLIENT_SECRET` | Google OAuth client secret | No       |

### Email (SMTP)

| Variable      | Description        | Required |
| ------------- | ------------------ | -------- |
| `SMTP_HOST`   | SMTP server host   | Yes      |
| `SMTP_PORT`   | SMTP server port   | Yes      |
| `SMTP_SECURE` | Use TLS/SSL        | Yes      |
| `SMTP_USER`   | SMTP username      | Yes      |
| `SMTP_PASS`   | SMTP password      | Yes      |
| `SMTP_FROM`   | From email address | Yes      |

### Redis

| Variable     | Description       | Default     |
| ------------ | ----------------- | ----------- |
| `REDIS_HOST` | Redis server host | `localhost` |
| `REDIS_PORT` | Redis server port | `6379`      |

### Sentry (Optional)

| Variable     | Description                   | Required |
| ------------ | ----------------------------- | -------- |
| `SENTRY_DSN` | Sentry DSN for error tracking | No       |

### OAuth (Google)

| Variable               | Description                | Required |
| ---------------------- | -------------------------- | -------- |
| `APP_URL`              | Application base URL       | Yes      |
| `GOOGLE_CLIENT_ID`     | Google OAuth client ID     | No       |
| `GOOGLE_CLIENT_SECRET` | Google OAuth client secret | No       |
| `GOOGLE_CALLBACK_URL`  | OAuth callback URL         | No       |

### Rate Limiting

Rate limiting is enabled via Redis:

| Endpoint Type | Limit   | Window |
| ------------- | ------- | ------ |
| Auth          | 10/min  | per IP |
| API           | 100/min | per IP |
| Bulk          | 20/min  | per IP |

### Server Configuration

| Variable      | Description                          | Default       |
| ------------- | ------------------------------------ | ------------- |
| `PORT`        | Server port                          | `3000`        |
| `NODE_ENV`    | Environment (development/production) | `development` |
| `CORS_ORIGIN` | CORS allowed origins                 | `*`           |
| `TZ`          | Timezone for date operations         | `UTC`         |

## Project Structure

```text
src/
├── api/             # Route handlers and controllers
│   ├── auth/        # Authentication (Better Auth), roles
│   ├── customers/   # Customer CRUD, analytics, lifecycle
│   ├── segments/    # Customer segmentation
│   ├── orders/      # Order management
│   ├── products/    # Product management
│   ├── imports/     # CSV/XLSX import jobs
│   ├── exports/     # Export jobs
│   ├── integrations/# Shopify integration, sync, webhooks
│   └── cron/        # Background job triggers
├── config/          # Configuration (env, prisma, roles)
├── middlewares/     # Auth, validation, and error handling
├── queues/          # BullMQ background workers
├── scripts/        # Seeding and utility scripts
├── utils/           # Shared utilities (logger, parser)
└── types/          # TypeScript type definitions
```

## Rate Limiting

Rate limiting is enabled via Redis:

| Endpoint        | Limit   |
| --------------- | ------- |
| Auth endpoints  | 10/min  |
| API endpoints   | 100/min |
| Bulk operations | 20/min  |

## Sentry Integration

Set `SENTRY_DSN` environment variable to enable error tracking in production.

## Redis

Redis is required for:

- Session storage (Better Auth)
- Rate limiting
- Background job queues (BullMQ)
- Webhook idempotency keys

Redis is optional - the API will run with synchronous fallbacks if Redis is not available.
